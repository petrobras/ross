repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
  # Ruff version — keep in sync with the ruff pin in pyproject.toml (dev extras).
    rev: v0.16.3
    hooks:
      # Run the linter.
      - id: ruff
      # Run the formatter.
      - id: ruff-format