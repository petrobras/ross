A quick tutorial on how to use ROSS
===================================

.. _tutorial:

Downloading ROSS
----------------

From the command line, execute:

::

    git clone https://github.com/ross-rotordynamics/ross.git

If you are on Windows, you can execute git commands such as this one by running
Git Bash instead of Git GUI. See :ref:`introduction` for more information on how
to install Git.