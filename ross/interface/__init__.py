"""The ROSS graphical interface: a local web application over the library.

Installed with ``pip install "ross-rotordynamics[interface]"`` and started
with ``ross-interface`` (or ``python -m ross.interface``). The Flask server
listens on the loopback interface only and opens the default browser on it.
"""
