version: 2

build:
  os: ubuntu-22.04

python:
  version: "3.10"
  install:
      - method: pip
        path: .
      - requirements: docs/requirements.txt

sphinx:
  builder: html
  fail_on_warning: false